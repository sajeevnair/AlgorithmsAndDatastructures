#include <iostream>
#include <fstream>
#include <ctime>
#define REPEAT 100000

using namespace::std;


void printArray(int*, int);
void printArray(char*, int);
void swap(int& , int&);
void swap(char&, char&);
void bubbleSort(int *, int, bool);
